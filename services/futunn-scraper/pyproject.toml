[project]
name = "futunn-scraper"
version = "0.1.0"
description = "Scrapes financial news from Futu NN 7x24 flash"
requires-python = ">=3.11"
dependencies = [
  "finance-shared",
  "httpx>=0.27.0",
]

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"

[tool.uv.sources]
finance-shared = { workspace = true }
