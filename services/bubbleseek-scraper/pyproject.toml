[project]
name = "bubbleseek-scraper"
version = "0.1.0"
description = "Scrapes financial news and options flow from BubbleSeek"
requires-python = ">=3.11"
dependencies = [
  "finance-shared",
  "httpx>=0.27.0",
]

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"

[tool.uv.sources]
finance-shared = { workspace = true }
