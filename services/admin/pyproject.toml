[project]
name = "admin-ui"
version = "0.1.0"
description = "Admin UI for Finance MCP"
requires-python = ">=3.11"
dependencies = [
  "finance-shared",
  "python-dotenv>=1.0.0",
  "sqladmin[full]",
  "starlette",
  "uvicorn",
]

[tool.uv.sources]
finance-shared = { workspace = true }
