[project]
name = "admin"
version = "0.1.0"
description = "Admin for Finance MCP"
requires-python = ">=3.11"
dependencies = [
  "finance-shared",
  "sqladmin[full]",
  "fastapi",
  "uvicorn",
]

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"

[tool.uv.sources]
finance-shared = { workspace = true }
